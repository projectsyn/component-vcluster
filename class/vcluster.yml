parameters:
  kapitan:
    compile:
      - input_paths:
          - vcluster/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - vcluster/component/main.jsonnet
        input_type: jsonnet
        output_path: ${_instance}/
