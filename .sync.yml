:global:
  componentName: vcluster
  githubUrl: https://github.com/projectsyn/component-vcluster
  feature_goldenTests: true
  testMatrix:
    key: instance
    entries:
      - defaults
      - oidc
      - openshift

docs/antora.yml:
  name: vcluster
  title: vcluster
.github/workflows/test.yaml:
  test_makeTarget: test -e instance=${{ matrix.instance }}
  goldenTest_makeTarget: golden-diff -e instance=${{ matrix.instance }}
Makefile:
  includes:
    - Makefile.custom.mk
